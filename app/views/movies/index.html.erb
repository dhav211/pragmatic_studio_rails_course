<h2>Movies</h2>
<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= movie.title %>
      (<%= movie.rating %>):
      <%= total_gross(movie) %>
    </li>
    <p><%= truncate(movie.description, length: 40, separator: ' ') %></p>
    <p><%= year_released(movie) %></p>
  <% end %>
</ul>